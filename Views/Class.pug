extends layout

block content
  h1 #{classItem.className}
  p Level: #{classItem.level}
  a(href=`/class/${classItem._id}/edit`) Edit
  form(action=`/class/${classItem._id}?_method=DELETE`, method='POST')
    button(type='submit') Delete

